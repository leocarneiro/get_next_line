
on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
      working-directory: ./Get_Next_Line_Tester
    - name: run
      run: bash run.sh
      working-directory: ./Get_Next_Line_Tester
